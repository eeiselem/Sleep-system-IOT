/**
 * Copy this file to `secrets.h` in the same folder (same name as sketch).
 * `secrets.h` is gitignored — never commit real Wi‑Fi credentials.
 *
 * Arduino IDE / PlatformIO: place `secrets.h` next to `ESP32.ino`.
 */
#ifndef ESP32_SECRETS_H
#define ESP32_SECRETS_H

#define WIFI_SSID "your-wifi-ssid"
#define WIFI_PASSWORD "your-wifi-password"

#endif
